# Core dependencies
numpy>=2.0.0
# page-dewarp depends on opencv-python. Keep one cv2 provider in packaged
# environments instead of installing both full and headless OpenCV wheels.
opencv-python>=4.10.0

# Fast, lossless image->PDF wrapper (used to avoid slow pdf-lib conversion).
img2pdf>=0.6.3
Pillow>=10.0.0

# Dewarping (optional but recommended)
page-dewarp>=0.2.7

# Build tool
pyinstaller>=6.0.0
