[package]
name = "evb-pdf-image-combine"
version = "0.1.0"
edition = "2021"

[dependencies]
crc32fast = "1.5.0"
flate2 = { version = "1.1.9", default-features = false, features = ["zlib-rs"] }
tiff = "0.11.3"
